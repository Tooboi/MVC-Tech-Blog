    <div class="">
        <h2 class="pb-4">Hey {{name}}</h2>
        <a href="/dashboard/create/"><button id="create-post" class="btn btn-primary">New Post</button></a>
        <hr>
       
{{#if post.length}}
    </div> 
    {{#each post}}
        {{> post-data post}}

            <a href="/dashboard/edit/{{post.id}}"><button class="btn mb-5 ml-5 btn-primary">edit post</button></a>
        </div>
    {{/each}}
{{else}}
<h6 class="pb-4">nothing to see here - try posting something</h6>
{{/if}}
